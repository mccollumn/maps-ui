import React from "react";

export const Users = ({children}) => {
  return (
    <div>
      I am USERS ddddd
      <div
        style={{
          color: 'red'
        }}>
        {children}
      </div>
    </div>
  );
};
